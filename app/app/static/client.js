class Client {
    // == Navigation ==
    static go_to_homepage() {
        window.location.replace("/");
    }

    static go_to_login() {
        window.location.replace("/login");
    }

    // == Database ==
}
